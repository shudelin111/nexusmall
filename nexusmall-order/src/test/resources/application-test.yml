# =====================================================
# NexusMall 订单服务 - 测试环境本地配置
# =====================================================
# 说明：
# - 此文件仅在 profile 为 test 时生效
# - 与 bootstrap.yml 配合使用，bootstrap.yml 负责连接 Nacos
# - Nacos 会加载 nexusmall-order-test.yaml（包含数据源等配置）
# - 此文件用于覆盖或补充 Nacos 中没有的配置
# =====================================================

# ===============================
# Spring Boot Actuator 监控配置
# ===============================
management:
  metrics:
    export:
      simple:
        enabled: false
