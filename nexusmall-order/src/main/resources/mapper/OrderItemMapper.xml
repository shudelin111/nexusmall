<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.nexusmall.order.dao.OrderItemMapper">

    <resultMap id="BaseResultMap" type="com.nexusmall.order.entity.OrderItem">
        <id column="id" property="id"/>
        <result column="order_id" property="orderId"/>
        <result column="order_sn" property="orderSn"/>
        <result column="sku_id" property="skuId"/>
        <result column="sku_name" property="skuName"/>
        <result column="sku_price" property="skuPrice"/>
        <result column="quantity" property="quantity"/>
        <result column="subtotal" property="subtotal"/>
        <result column="create_time" property="createTime"/>
    </resultMap>

    <sql id="Base_Column_List">
        id, order_id, order_sn, sku_id, sku_name, sku_price, quantity, subtotal, create_time
    </sql>

    <select id="selectById" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM `order_item`
        WHERE id = #{id}
    </select>

    <select id="listByOrderId" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM `order_item`
        WHERE order_id = #{orderId}
    </select>

    <select id="listByOrderSn" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM `order_item`
        WHERE order_sn = #{orderSn}
    </select>

    <insert id="insert" parameterType="com.nexusmall.order.entity.OrderItem" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO `order_item` (
            order_id, order_sn, sku_id, sku_name, sku_price, quantity, subtotal, create_time
        ) VALUES (
            #{orderId}, #{orderSn}, #{skuId}, #{skuName}, #{skuPrice}, #{quantity}, #{subtotal}, #{createTime}
        )
    </insert>

    <insert id="batchInsert">
        INSERT INTO `order_item` (
            order_id, order_sn, sku_id, sku_name, sku_price, quantity, subtotal, create_time
        ) VALUES
        <foreach collection="list" item="item" separator=",">
            (
                #{item.orderId}, #{item.orderSn}, #{item.skuId}, #{item.skuName},
                #{item.skuPrice}, #{item.quantity}, #{item.subtotal}, #{item.createTime}
            )
        </foreach>
    </insert>

    <delete id="deleteByOrderId">
        DELETE FROM `order_item`
        WHERE order_id = #{orderId}
    </delete>

    <delete id="batchDelete">
        DELETE FROM `order_item`
        WHERE id IN
        <foreach collection="list" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
    </delete>
</mapper>
